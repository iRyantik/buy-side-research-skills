# Examples

Examples are compact research workspaces that show how the plugin output should look after real research work.

They are not runtime dependencies. Skills must not rely on files under `examples/`; any runtime template, script, or reference needed by a skill should live inside that skill directory.

Current example:

- `workspaces/ai-data-center-power/`: migrated sample workspace from the original mixed plugin/research repo layout.
