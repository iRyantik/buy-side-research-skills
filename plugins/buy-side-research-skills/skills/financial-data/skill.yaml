metadata_schema_version: 1
name: financial-data
id: financial-data
display_name: Financial Data
version: 2.0.0
system_generation: 6.0.0
author: buy-side-research-system
namespace: research.equity
category: operations
summary: Acquire evidence and commit source-tracked canonical financial facts.
description: "Acquire evidence and commit source-tracked canonical financial facts."
trigger:
  type: keyword
  keywords: [financial-data, fetch financial data, 拉结构化财报, 拉财务数据]
capabilities:
  modes: [Lite Fetch, Full Fetch, Arbitrary Time Range, Migration, Render]
  requirements:
    - lite and full control acquisition depth but not requested time range
    - only the canonical facts repository writes facts-store.json
    - generate actuals-resolved.json as a read-only compatibility view
    - preserve source evidence and conflicts
quality_gates:
  anti_patterns:
    - direct_actuals_writer
    - silent_zero_fill
    - silent_lower_trust_overwrite
  required_outputs:
    - internal/facts-store.json
    - internal/actuals-resolved.json
    - dataset run manifest
