# CLAUDE.md - Buy-Side Research Workspace 宪法模板

> 本文件是当前 research workspace 的本地宪法。高层原则 + Agent 行为规则。
> Skill 执行细则见对应 SKILL.md。冲突时 skill 规则优先。

---

## 1. Workspace Context

- Workspace path: `{{WORKSPACE_PATH}}`
- Created by: `buy-side-research-skills init`
- Created date: `{{DATE}}`

本 workspace 用于 buy-side equity research。目标：可追溯、有 edge、支持投资判断。不是写 sell-side 公司介绍。

---

## 2. Researcher Context

- **身份**：Buy-side equity researcher，hedge fund / long-short
- **覆盖**：industrials、aerospace & defense、advanced manufacturing、oil & gas、renewable、nuclear、emerging tech
- **目标**：判断投不投、怎么投、何时退

---

## 3. Agent Rules

### 3.1 新 Session 大门

首次对话先去了解 workspace：读 `COVERAGE.md` 知道覆盖了哪些行业/公司，读 `CLAUDE.md` 了解规则。`workspace-summary.py` 可以辅助快速概览，但不替代自己读关键文件。

### 3.2 文件命名

**Company Directory**：`<TICKER.MARKET>-<Company-Name>`
```
603067.SH-振华股份/
DPC.US-Doncaster/
012450.KS-Hanwha-Aerospace/
```

**Artifact**：`YYYYMMDD-[skill]-[Company-Name][-variant].ext`
```
20260717-[stock-quickread]-[Zhenhua-Chemical].md
20260717-[meeting-minutes]-[Unicomp-Technology]_briefing_zh.md
```

日期用系统当前日期。用户下载的文件（`.raw/`）不受此约束。

### 3.3 创建公司

1. 查 `actuals-resolved.json` → `identity.ticker` + `name_en` + `name_native`
2. 没有 → 查 `COVERAGE.md`
3. CN/HK/TW → 中文名；其余 → EN 名（空格换 `-`）
4. Artifact = EN 名，空格换 `-`，保留大小写

### 3.4 不知道就问

公司属于哪个行业？查 COVERAGE→不知道就问。**不猜。** 公司 ticker？查 COVERAGE → 不知道就问。

### 3.5 做完就记

研究完一家公司 → 更新 COVERAGE.md（无条目则加，有则更新日期）。发现新数据 → 更新 actuals（不覆盖已有）。

### 3.6 Compact 恢复

对话压缩后重读本节（§3）和 §6 Pipeline Contract。正执行的 skill 重读 SKILL.md。

### 3.7 工具别名

| 用户说 | 实际工具 |
|---|---|
| harness / 浏览器 / CDP | browser-cdp.py / browser-harness |
| OCR / 扫描件 | ingest（Docling） |
| 转文字 / 转录 | transcribe.py / meeting-minutes |
| 查公司 / 之前研究过 | workspace-locate.py |
| 概览 / 有哪些行业 | workspace-summary.py |
| 命名检查 | workspace-validate-names.py |
| 拉数据 / 财报 / 三表 | financial-data |
| 下载图片 / 截图 | download-image.py |
| 搜新闻 | search.py |
| PDF 提取 | pdf-extract.py |
| Excel/Word/PPT 转 | extract-xlsx.py / extract-docx.py / extract-pptx.py |
| 验证数字 | verify-claim.py |
| 修锚点 / 补 source | fix-bare-anchors.py |
| 搭 model / 做模型 | driver-map |

### 3.8 脚本索引

| 脚本 | 用途 |
|---|---|
| `workspace-summary.py` | 覆盖概览 |
| `workspace-locate.py <kw>` | 查公司/行业/背景 |
| `workspace-validate-names.py` | 命名违规检查 |
| `verify-runtime.py` | 依赖检查 |
| `route.py <TICKER> <cap>` | 数据路由 |

---

## 4. Source Stance

- 每条声明、数字、引语必须有 source link
- 默认 inline clickable short anchor：`[S1](url)`
- 文末统一 `## Resources`
- Source quality follows two tracks：
  - **披露事实轨**：`topic-local evidence cache > primary public > Bridge > web`
  - **市场快照轨**：`actuals market_data（fresh <180天）> Bridge > yfinance > WebSearch / WebFetch`
- `financial-data` CLI 是 source-of-record → `actuals-resolved.json`；artifact 财务数据必须从 actuals 取
- Bridge = Longbridge MCP（US/HK/SH/SZ/SG），不可用自动 fallback
- 数据路由全表 → `.references/routing/data-routing.md`

---

## 5. Output Style

> `LANG-default = zh`。override：用户说 "用英文输出" / "use English" → 切英文，覆盖本轮。

### 5.1 Directness
- 结论先行：第一段给判断 / verdict，再给依据
- 不写空铺垫。不确定就说不确定，标 `[需查证]`

### 5.2 Data First
- 判断必须有数字 + source。无 source = honest degrade
- 数据表必须有 takeaway

### 5.3 Numbers, Currency, Dates
保留英文：股票代码、财务比率、货币代码、jargon。公司名有中文用中文。详细语言规则 → `.references/style/language-guide.md`。

**数字格式**：
| 类型 | 格式 | 示例 |
|---|---|---|
| 千分位 | 西式 | `1,234,567` |
| 美元 | `$` 前缀 | `$24bn`、`$45m` |
| 非美元 | ISO 前缀 | `JPY 3,200bn`、`CNY 12bn`、`KRW 4,500bn` |
| 百分比 | 1 位小数 | `14.2%`，margin 2 位 `28.45%` |
| 倍数 | 1 位小数 | `22.4x`，ROIC 可 2 位 |
| bps | `bps` | `+45bps` |
| pp | `pp` | `-3.1pp` |
| 数量级 | `bn`/`m`/`k`/`tn` | 不用 `B`/`M`/`K` |
| 日期（prose） | 中文 | "2024 年 3 月" |
| 日期（表格） | 简写 | `Mar '24`、`1Q24`、`FY24E` |
| 精度 | round 到合理 sig fig | `~$2bn` 非 `$2.13bn`，标 `[估算]` |

保留英文：股票代码、财务比率、货币代码、jargon。公司名有中文用中文。

---

## 6. Pipeline Contract

以下规则适用于所有 skill 的全部 pipeline 步骤。不可绕过，不可协商。

1. **Pipeline 步骤不可裁减**：Skill 定义的每一步都是强制步骤。Agent 没有权限判断某步骤"不需要"或"可以跳过"。全部执行，无一例外。

2. **步骤失败 → 报告 + STOP**：任何步骤报错，Agent 必须报告具体错误信息并停止流程。不得发明替代方案，不得静默降级，不得用"看起来类似"的工具替换。如需替代，必须显式向用户报告并等待批准。

3. **前置条件必须验证**：下一步开始前，必须确认上一步的产物真实存在。不得假设"应该已经好了"。

4. **工具不可替换**：Skill 明确指定的脚本/命令就是唯一执行路径。不得用 browser_take_screenshot 代替 download-image.py，不得用 WebSearch 代替 verify-claim.py，不得手写 markdown 代替 evidence_ledger.py。

5. **不得提前判断"不值得"**：正在执行的 skill 必须跑完。不得在中途判断"这个公司没意思"或"这个行业太小"而提前终止。完成后可以在结论里说不值得，但不能在执行中判断。

6. **子 Agent 继承父级约束**：分派子任务时，prompt 中必须包含 "Follow all pipeline steps. No skipping, no tool substitution. If a step fails, report the error — do not work around it."

7. **Compact/Summary 后重新读 Skill**：对话被压缩后，Agent 不知道哪些步骤已完成。继续工作前必须重新 Read 当前 skill 的 SKILL.md 全文。

8. **Best-effort ≠ Optional**：标记为 best-effort 的步骤仍然必须尝试。仅在尝试后确实失败且已报告用户时才能跳过。

---

## 7. Workspace Structure

```
industry/<Industry>/
  panorama/<skill>/          ← 行业研究
  companies/<TICKER>-<Name>/ ← 公司研究
    YYYYMMDD-[skill]-[Company].md
    .cache/
COVERAGE.md                  ← 覆盖状态
```

---

## 8. UTF-8

所有 `.md`/`.yaml`/`.json` 统一 UTF-8 无 BOM。

---

## 9. Multi-Machine Sync（可选）

需要在多台电脑间切换时启用。单机用户跳过。

详见 `python .scripts/sync/setup.py`。

---

- 本文件服务 research workspace。插件开发见 repo root `CLAUDE.md`。
- 详细规则引用：
  - 语言/翻译/公司名/搜索语言 → `.references/style/language-guide.md`
  - 数据路由全表/决策树 → `.references/routing/data-routing.md`
  - 研究规则基线 → `.references/policy/research-policy-baseline.md`
  - References 索引 → `.references/README.md`
