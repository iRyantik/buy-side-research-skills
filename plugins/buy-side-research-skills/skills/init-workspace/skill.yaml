metadata_schema_version: 1
name: init-workspace
id: init-workspace
display_name: Init Research Workspace
version: 2.0.0
system_generation: 6.0.0
author: buy-side-research-system
namespace: research.equity
category: operations
summary: Initialize or repair a workspace from the explicit managed runtime payload.
description: "Initialize or repair a research workspace using the manifest-managed runtime payload."
trigger:
  type: keyword
  keywords: [init research workspace, 初始化研究工作区, repair research workspace]
capabilities:
  modes: [New Workspace Scaffold, Repair Existing Workspace, Plan Only]
  requirements:
    - require an explicit user workspace before mutating files
    - deploy only runtime/managed-assets.json entries
    - preserve unknown and user-modified files
    - run runtime-manager verify after apply
quality_gates:
  anti_patterns:
    - recursive_skill_asset_copy
    - overwrite_user_files
    - initialize_inside_plugin_repo
  required_scaffold:
    - .research-runtime/installed-manifest.json
    - _scripts/source-intake.py
    - _scripts/financial-data.py
    - _scripts/runtime-manager.py
    - .claude/hooks/hook_entry.py
